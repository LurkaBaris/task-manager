# Task Manager

Канбан-доска для управления задачами. Приложение хранит данные локально в браузере через IndexedDB, поддерживает создание, редактирование, удаление, восстановление удаленной задачи, поиск, фильтрацию, сортировку, drag and drop и импорт/экспорт задач через JSON-файл

## Стек

- React 19
- TypeScript
- Vite
- React Router
- Zustand
- IndexedDB + idb
- Mantine UI
- dnd-kit
- React Hook Form + Zod
- CSS Modules

## Возможности

- Создание задачи с названием, описанием, статусом и приоритетом
- Редактирование и удаление задач
- Undo удаления через notification
- Поиск по названию и описанию с debounce
- Фильтрация по приоритетам
- Сортировка задач внутри колонок
- Drag and drop задач внутри колонки и между колонками
- Нормализация позиций задач при изменении порядка
- Локальное хранение задач в IndexedDB
- Импорт и экспорт списка задач через JSON-файл
- Error Boundary и уведомления для пользовательских действий

## Drag and drop

Drag and drop реализован не только как визуальное перемещение карточек, но и как полноценная логика изменения порядка задач

Каждая колонка может иметь собственный режим сортировки: ручной порядок, новые задачи сначала или старые задачи сначала. Выбранный режим сохраняется в `localStorage`, поэтому при перетаскивании учитывается не только колонка назначения, но и текущий способ отображения задач в ней

Для ручного порядка используется поле `position`. Позиции хранятся с запасом между значениями, например `0`, `1000`, `2000`. Благодаря этому при вставке задачи между двумя соседними задачами можно выдать ей промежуточную позицию, например `1500`, без массового пересчета всех задач в IndexedDB

Когда свободного промежутка между соседними позициями становится недостаточно, запускается нормализация: задачи в колонке сортируются и получают новые позиции с нормальным шагом. Это сохраняет быстрые вставки и не дает позициям превратиться в трудно управляемый набор значений

Во время перетаскивания используется draft-состояние, чтобы интерфейс сразу показывал будущий результат перемещения. После завершения drag and drop итоговое состояние сохраняется в хранилище

## Запуск проекта

Установить зависимости:

```bash
npm i
```

Запустить dev-сервер:

```bash
npm run dev
```

После запуска Vite покажет локальный адрес приложения в терминале.

## Проверки

Проверить TypeScript и собрать production build:

```bash
npm run build
```

Запустить ESLint:

```bash
npm run lint
```

Автоматически исправить доступные lint-ошибки:

```bash
npm run lint:fix
```

Проверить форматирование:

```bash
npm run format:check
```

Отформатировать проект:

```bash
npm run format
```

## Preview production build

Сначала собрать проект:

```bash
npm run build
```

Затем запустить preview:

```bash
npm run preview
```

## Данные приложения

Задачи сохраняются локально в IndexedDB браузера. Данные не отправляются на сервер и доступны только в текущем браузере пользователя

Для переноса или резервного копирования задач можно использовать:

- `Экспорт` - выгружает текущий список задач в JSON-файл
- `Импорт` - загружает задачи из JSON-файла

При импорте доступны два режима:

- `Обновить` - добавляет новые задачи и обновляет существующие по `id`
- `Заменить все` - полностью заменяет текущий список задач данными из файла

## Структура проекта

Проект организован по FSD:

```txt
src/app        - инициализация приложения, провайдеры, роутинг, layout
src/pages      - страницы приложения
src/widgets    - крупные UI-блоки, например доска задач
src/features   - пользовательские сценарии: создание, редактирование, удаление, импорт, экспорт, dnd
src/entities   - бизнес-сущности: task, column
src/shared     - общие настройки и утилиты
```
