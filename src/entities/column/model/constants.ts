import { COLUMN_COLOR_OPTIONS } from '@/shared/config'

export {
  COLUMN_COLOR_OPTIONS,
  COLUMN_ORDER_STEP,
  DEFAULT_COLUMN_COLOR,
  DEFAULT_COLUMNS,
} from '@/shared/config'

export const COLUMN_COLOR_VALUES = COLUMN_COLOR_OPTIONS.map((option) => option.value)
